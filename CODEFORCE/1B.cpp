#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        string s;
        cin >>s;
        
        //check form
        if(s[0] == "R" && s.find("C") != string::npos){
            int i = 1;
            while(i)
        }
    }
}